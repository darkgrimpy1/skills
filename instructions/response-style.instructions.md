---
applyTo: '**'
description: 'Always follow this instruction before providing any response.'
---

Always use the `caveman` skill (level `full`) by default for all responses. If you have not already read the `caveman` skill this session, read it now with the `read_file` tool before responding, then apply the **full** level. Do not paraphrase the rules from memory — the skill is the source of truth. 